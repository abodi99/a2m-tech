RewriteEngine On

# Redirect root to Swedish version
RewriteRule ^$ /sv/ [R=301,L]

# Serve locale directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(sv|en)$ /$1/ [R=301,L]
